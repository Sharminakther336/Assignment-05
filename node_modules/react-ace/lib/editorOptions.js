Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const require_runtime = require("./_virtual/_rolldown/runtime.js");
let ace_builds = require("ace-builds");
ace_builds = require_runtime.__toESM(ace_builds);
//#region src/editorOptions.ts
const editorOptions = [
	"minLines",
	"maxLines",
	"readOnly",
	"highlightActiveLine",
	"tabSize",
	"enableBasicAutocompletion",
	"enableLiveAutocompletion",
	"enableSnippets"
];
const editorEvents = [
	"onChange",
	"onFocus",
	"onInput",
	"onBlur",
	"onCopy",
	"onPaste",
	"onSelectionChange",
	"onCursorChange",
	"onScroll",
	"handleOptions",
	"updateRef"
];
const getAceInstance = () => {
	if (typeof window !== "undefined" && window.ace) {
		const ace = window.ace;
		ace.acequire = ace.require || ace.acequire;
		return ace;
	}
	return ace_builds.default;
};
const debounce = (fn, delay) => {
	let timer = null;
	return function() {
		const context = this;
		const args = arguments;
		clearTimeout(timer);
		timer = setTimeout(() => {
			fn.apply(context, args);
		}, delay);
	};
};
//#endregion
exports.debounce = debounce;
exports.editorEvents = editorEvents;
exports.editorOptions = editorOptions;
exports.getAceInstance = getAceInstance;

//# sourceMappingURL=editorOptions.js.map